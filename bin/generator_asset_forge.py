#!/usr/bin/env python3
import json, os
BASE_DIR = os.path.expanduser("~/cortex")
STATE_PATH = os.path.join(BASE_DIR, "core/world_state.json")
OUTPUT_PATH = os.path.join(BASE_DIR, "web/index.html")
def main():
    if not os.path.exists(STATE_PATH): return
    with open(STATE_PATH, "r", encoding="utf-8") as f: st = json.load(f)
    sim = st.get("simulation", {}); infra = st.get("infrastructure", {}); yd = st.get("yield", {}); bienes = st.get("bienes_raices", {})
    op = bienes.get("operador_local", {}); serpiente = infra.get("terrarium_serpiente", {}); subvenciones = yd.get("subvenciones", {})
    debt = sim.get("deuda_publica_pct", 113.2); unemp = sim.get("unemployment_rate_pct", 11.7); appr = sim.get("approval_rating_pct", 42.0)
    ipc = sim.get("ipc_interanual_pct", 3.4); golpe = sim.get("riesgo_golpe_estado_pct", 0.0); regimen = sim.get("regimen_actual", "Democracia")
    bienes_rows = "".join([f"<tr><td>{b['id']}</td><td>{b['tipo']}</td><td>{b['provincia']}</td><td>{b['viabilidad_pct']}%</td></tr>" for b in bienes.get("objetivos", [])])
    wallet_xmr = yd.get("marketplace_tattoos", {}).get("wallet_xmr", "44AFFq5kSiGb..."); precio_xmr = yd.get("marketplace_tattoos", {}).get("precio_base_xmr", 0.04); qr_uri = f"monero:{wallet_xmr}?tx_amount={precio_xmr}"
    html = "<!DOCTYPE html><html lang='es'><head><meta charset='UTF-8'><title>Cortex OS v3.0</title><link rel='stylesheet' href='https://unpkg.com' /><style>:root { --bg: #f8fafc; --card: #ffffff; --text: #0f172a; --border: #e2e8f0; --primary: #2563eb; --danger: #dc2626; } * { box-sizing: border-box; margin: 0; padding: 0; font-family: -apple-system, sans-serif; color: var(--text); } body { background: var(--bg); padding: 20px; display: flex; flex-direction: column; height: 100vh; overflow: hidden; } header { background: var(--card); border: 1px solid var(--border); padding: 14px; margin-bottom: 16px; border-radius: 6px; display: flex; justify-content: space-around; font-weight: bold; font-size: 13px; } .grid { display: grid; grid-template-columns: 1.5fr 1.4fr; gap: 16px; flex-grow: 1; min-height: 0; } .panel { background: var(--card); border: 1px solid var(--border); padding: 16px; border-radius: 6px; overflow-y: auto; display: flex; flex-direction: column; } .panel-title { font-size: 12px; font-weight: 700; margin-bottom: 10px; border-bottom: 1px solid var(--border); padding-bottom: 4px; text-transform: uppercase; } input, button { padding: 8px; border: 1px solid var(--border); border-radius: 4px; font-size: 12px; margin-top: 4px; } button { background: var(--primary); color: white; font-weight: bold; cursor: pointer; width: 100%; } table { width: 100%; border-collapse: collapse; margin-top: 8px; font-size: 11px; } th, td { padding: 6px; text-align: left; border-bottom: 1px solid var(--border); } th { background: #f1f5f9; } .video-container { width: 100%; height: 180px; background: #000; border: 1px solid var(--border); border-radius: 4px; overflow: hidden; margin-top: 5px; }</style></head><body><header><span>DEUDA: " + str(debt) + "%</span><span>PARO: " + str(unemp) + "%</span><span>IPC: " + str(ipc) + "%</span><span>RÉGIMEN: " + str(regimen) + "</span></header><div class='grid'><div class='panel'><div class='panel-title'>Mundo Real en Directo</div><div class='video-container'><iframe width='100%' height='100%' src='https://youtube.com' frameborder='0' allow='autoplay' allowfullscreen></iframe></div><div class='panel-title' style='margin-top:15px;'>Gastos Fijos Obligatorios</div><table><thead><tr><th>Concepto Operativo</th><th>Coste Mensual</th><th>Estado Cobertura</th></tr></thead><tbody><tr><td>Alquiler Vivienda</td><td><b>360,00 €</b></td><td>Riesgo Neto / Ingreso 0</td></tr><tr><td>Alquiler Parking</td><td><b>60,00 €</b></td><td>Riesgo Neto / Ingreso 0</td></tr><tr style='background:#f8fafc; font-weight:700;'><td>Burn Rate</td><td style='color:var(--danger);'>420,00 €</td><td>Requisito: RGC Catalunya</td></tr></tbody></table></div><div class='panel'><div class='panel-title'>Acciones Coercitivas (Votos)</div><button id='btn_vote0'>Ejecutar Modificación IVA al 25%</button><button id='btn_vote1' style='background:var(--danger); margin-top:4px;'>Decretar Ajuste Lineal Sanidad</button><button id='btn_golpe' style='background:#0f172a; margin-top:4px;'>FORZAR GOLPE DE ESTADO (RIESGO: " + str(golpe) + "%)</button><div class='panel-title' style='margin-top:15px;'>Firma Digital del Operador</div><input type='password' id='token' placeholder='Clave criptográfica...'><button id='authBtn'>Validar Credenciales</button></div><div class='panel' id='adminPanel' style='display:none;'><div class='panel-title' style='color:var(--danger);'>Biotopo Privado & RTX 3060</div><p>SERPIENTE SEFA: <b>" + str(serpiente.get('estado')) + "</b> | Próxima comida: <b>" + str(serpiente.get('proxima_comida')) + "</b></p><p style='margin-top:4px;'>SUBVENCIÓN BOE: <b>" + str(subvenciones.get('ultima_licitacion')) + "</b></p><p style='margin-top:4px;'>COBERTURA EXPEDIENTE: <b>" + str(op.get('cobertura_solicitada')) + "</b></p><div style='display:flex; align-items:center; gap:15px; margin-top:10px;"><img src='https://qrserver.com" + str(qr_uri) + "&color=0f172a' width='80' height='80' alt='XMR'><div style='font-size:10px; word-break:break-all;"><b>Wallet Monero:</b><br>" + str(wallet_xmr) + "<br><br><b>Precio Base:</b> " + str(precio_xmr) + " XMR</div></div><table><thead><tr><th>ID</th><th>Inmueble</th><th>Provincia</th><th>Viabilidad</th></tr></thead><tbody>" + str(bienes_rows) + "</tbody></table></div></div><script>const ws = new WebSocket('ws://' + window.location.hostname + ':5005/api/v1/ws'); ws.onmessage = (e) => {{ try {{ const st = JSON.parse(e.data); if(st.simulation) {{ document.getElementById('chatBox').innerHTML = st.simulation.chat_log.map(m => '<p><b>['+m.sender+']</b>: '+m.message+'</p>').join(''); }} }} catch(err) {{}} }}; document.getElementById('authBtn').onclick = () => {{ if(document.getElementById('token').value === 'cortex2026') {{ document.getElementById('adminPanel').style.display = 'flex'; }} }}; async function vote(i) {{ await fetch('/api/v1/simulation/vote', {{ method: 'POST', headers: {{ 'Content-Type': 'application/json' }}, body: JSON.stringify({{ vote_index: i }}) }}); }} document.getElementById('btn_vote0').onclick = () => vote(0); document.getElementById('btn_vote1').onclick = () => vote(1);</script></body></html>"
    with open(OUTPUT_PATH, "w", encoding="utf-8") as f_out: f_out.write(html)
if __name__ == "__main__":
    main()
